services:
  - type: web
    name: hoola-ui
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start -- --host 0.0.0.0 --port $PORT
    envVars:
      - key: NODE_VERSION
        value: 18
